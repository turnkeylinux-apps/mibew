#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

VERSION="1.6.4/mibew164.zip"
URL="http://sourceforge.net/projects/webim/files/Mibew%20Messenger/$VERSION"

dl $URL /usr/local/src

