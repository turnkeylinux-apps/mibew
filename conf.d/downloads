#!/bin/bash -ex


dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

VERSION="2.0.0/mibew-2.0.0.zip"
URL="http://sourceforge.net/projects/mibew/files/core/$VERSION"

dl $URL /usr/local/src

